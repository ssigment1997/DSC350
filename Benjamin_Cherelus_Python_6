#Write a program that captures a string containing a person’s first, middle, and last names, and then display their first, middle, and last initials. For example, if the user enters John William Smith, the program should display J. W. S.

full_name = 'Austin Powell Senior'
first_name = full_name[0:1]
middle_name = full_name[7:8]
last_name = full_name[14:15]
print(f'{first_name}.{middle_name}.{last_name}.')  
